# -------------------------------------------------------------------
# ------------------------------- WARNING ---------------------------
# -------------------------------------------------------------------
#
# This file was automatically generated by gh-workflows using the
# gh-workflow-gen bin. You should add and commit this file to your
# git repository. **DO NOT EDIT THIS FILE BY HAND!** Any manual changes
# will be lost if the file is regenerated.
#
# To make modifications, update your `build.rs` configuration to adjust
# the workflow description as needed, then regenerate this file to apply
# those changes.
#
# -------------------------------------------------------------------
# ----------------------------- END WARNING -------------------------
# -------------------------------------------------------------------

name: ci
env:
  RUSTFLAGS: '-Dwarnings'
'on':
  pull_request:
    types:
    - opened
    - synchronize
    - reopened
    branches:
    - main
  push:
    branches:
    - main
jobs:
  build:
    name: Build and Test
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
    - name: Checkout Code
      uses: actions/checkout@v4
    - name: Install protoc and lcov
      run: sudo apt-get update && sudo apt-get install -y protobuf-compiler lcov
    - name: Start Test Infrastructure
      run: docker compose -f docker-compose.test.yml up -d && sleep 5
    - name: Setup Rust Toolchain
      uses: actions-rust-lang/setup-rust-toolchain@1780873c7b576612439a134613cc4cc74ce5538c
      with:
        toolchain: nightly
        components: llvm-tools-preview
        cache: 'true'
    - name: Cache cargo-llvm-cov
      uses: actions/cache@v4
      with:
        path: ~/.cargo/bin/cargo-llvm-cov
        key: ${{ runner.os }}-cargo-llvm-cov-${{ hashFiles('**/Cargo.lock') }}
        restore-keys: ${{ runner.os }}-cargo-llvm-cov-
    - name: Install cargo-llvm-cov
      run: cargo install cargo-llvm-cov || true
    - name: Generate coverage (main branch or expensive)
      if: '${{ github.ref == ''refs/heads/main'' || contains(github.event.pull_request.labels.*.name, ''ci: expensive'') }}'
      run: (cd fetcher-be && cargo +nightly llvm-cov --release --all-features --workspace --lcov --output-path ../lcov-fetcher.info) && (cd brainatlas-be && cargo +nightly llvm-cov --release --all-features --workspace --lcov --output-path ../lcov-brainatlas.info) && (cd orch && cargo +nightly llvm-cov --release --all-features --workspace --lcov --output-path ../lcov-orch.info)
    - name: Generate coverage (fast)
      if: '${{ github.ref != ''refs/heads/main'' && !contains(github.event.pull_request.labels.*.name, ''ci: expensive'') }}'
      run: (cd fetcher-be && cargo +nightly llvm-cov --workspace --lcov --output-path ../lcov-fetcher.info) && (cd brainatlas-be && cargo +nightly llvm-cov --workspace --lcov --output-path ../lcov-brainatlas.info) && (cd orch && cargo +nightly llvm-cov --workspace --lcov --output-path ../lcov-orch.info)
    - name: Merge coverage reports
      run: lcov --add-tracefile lcov-fetcher.info --add-tracefile lcov-brainatlas.info --add-tracefile lcov-orch.info --output-file lcov.info
    - name: Upload Coverage to Codecov
      uses: Wandalen/wretry.action@v3
      with:
        action: codecov/codecov-action@v4
        attempt_limit: '3'
        attempt_delay: '10000'
        with: |-
          token: ${{ secrets.CODECOV_TOKEN }}
          files: lcov.info
  lint:
    name: Lint
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
    - name: Checkout Code
      uses: actions/checkout@v4
    - name: Install protoc
      run: sudo apt-get update && sudo apt-get install -y protobuf-compiler
    - name: Setup Rust Toolchain
      uses: actions-rust-lang/setup-rust-toolchain@1780873c7b576612439a134613cc4cc74ce5538c
      with:
        toolchain: nightly
        components: clippy, rustfmt
    - name: Cargo Fmt
      run: (cd fetcher-be && cargo +nightly fmt --all --check) && (cd brainatlas-be && cargo +nightly fmt --all --check) && (cd orch && cargo +nightly fmt --all --check)
    - name: Cargo Clippy
      run: (cd fetcher-be && cargo +nightly clippy --all-features --workspace -- -D warnings) && (cd brainatlas-be && cargo +nightly clippy --all-features --workspace -- -D warnings) && (cd orch && cargo +nightly clippy --all-features --workspace -- -D warnings)
